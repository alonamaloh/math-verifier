*.o
*.tmp
kernel
build/

# clangd: generated compilation database (machine-specific absolute
# paths; regenerate with `make compile_commands.json`) and its
# background index cache.
compile_commands.json
.cache/

# Emacs autosave / lockfiles -- these have been accidentally committed
# more than once. Better to ignore explicitly.
\#*\#
.\#*
