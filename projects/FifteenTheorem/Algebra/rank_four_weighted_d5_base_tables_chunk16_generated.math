-- Generated by scripts/generate_rank_four_weighted_d5_covers.py.  Do not edit.
-- Explicit witnesses for the finite base cases below the uniform 5-adic tails.

module Algebra.rank_four_weighted_d5_base_tables_chunk16_generated

import Algebra.rank_four_weighted_d5_cover

theorem Matrix.weightedD5S1R0C9_small_ten_base_part0
        : ∀ (b : ℕ). b ≥ 0 → b < 1 →
          Matrix.Represents(Matrix.squarePlusDoublePlusScaledRankFourRepresentative(5, 1, 0, 9), ((25 * b + 10 : ℕ) : ℤ)) := {
    let A : Matrix(Integer.commutative_ring_bundle, 4, 4) := Matrix.squarePlusDoublePlusScaledRankFourRepresentative(5, 1, 0, 9);
    1 * 1 + 2 * (0 * 0) + 5 * (0 * 0) + 2 * 1 * 0 * 1 + 2 * 0 * 0 * 1 + 9 * (1 * 1) = (10 : ℤ) as scalarComputation;
    Matrix.represents_by_witness(A, target := 10, vector := ⟨1, 0, 0, 1⟩,
      computes := Matrix.squarePlusDoublePlusScaledRankFourRepresentative_coordinateTuple_computes(
        scalarComputation := scalarComputation));
    done by finite_check b from 0 until 1
}
theorem Matrix.weightedD5S1R0C9_small_ten_base
        : ∀ (b : ℕ). b ≥ 0 → b < 1 →
          Matrix.Represents(Matrix.squarePlusDoublePlusScaledRankFourRepresentative(5, 1, 0, 9), ((25 * b + 10 : ℕ) : ℤ)) := {
    done by finite_check b from 0 until 1
}
theorem Matrix.weightedD5S1R0C9_small_fifteen_base_part0
        : ∀ (b : ℕ). b ≥ 0 → b < 1 →
          Matrix.Represents(Matrix.squarePlusDoublePlusScaledRankFourRepresentative(5, 1, 0, 9), ((25 * b + 15 : ℕ) : ℤ)) := {
    let A : Matrix(Integer.commutative_ring_bundle, 4, 4) := Matrix.squarePlusDoublePlusScaledRankFourRepresentative(5, 1, 0, 9);
    1 * 1 + 2 * (0 * 0) + 5 * (1 * 1) + 2 * 1 * 0 * 1 + 2 * 0 * 1 * 1 + 9 * (1 * 1) = (15 : ℤ) as scalarComputation;
    Matrix.represents_by_witness(A, target := 15, vector := ⟨1, 0, 1, 1⟩,
      computes := Matrix.squarePlusDoublePlusScaledRankFourRepresentative_coordinateTuple_computes(
        scalarComputation := scalarComputation));
    done by finite_check b from 0 until 1
}
theorem Matrix.weightedD5S1R0C9_small_fifteen_base
        : ∀ (b : ℕ). b ≥ 0 → b < 1 →
          Matrix.Represents(Matrix.squarePlusDoublePlusScaledRankFourRepresentative(5, 1, 0, 9), ((25 * b + 15 : ℕ) : ℤ)) := {
    done by finite_check b from 0 until 1
}
