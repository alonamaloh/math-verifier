module corpus.unprovable_claim_auto

import axioms
import Logic.basics
import Equality.basics
import Natural.basics
import Natural.peano
import Natural.arithmetic

theorem bad : 0 = 0 := {
  claim 0 = successor(zero);
  reflexivity(zero)
}
