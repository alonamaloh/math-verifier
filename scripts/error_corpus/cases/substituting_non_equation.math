module corpus.substituting_non_equation

import axioms
import Logic.basics
import Equality.basics
import Natural.basics
import Natural.peano
import Natural.arithmetic

theorem bad (a : Natural) : a = a := {
  claim result : a = a by substituting (0 = 0 ∧ 0 = 0);
  result
}
