module corpus.calc_broken_step

import axioms
import Logic.basics
import Equality.basics
import Natural.basics
import Natural.peano
import Natural.arithmetic

theorem bad (a : Natural) : a = successor(a) :=
  calc a
     = successor(a)   by reflexivity(a)
