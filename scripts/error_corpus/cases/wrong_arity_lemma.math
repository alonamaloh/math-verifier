module corpus.wrong_arity_lemma

import axioms
import Logic.basics
import Equality.basics
import Natural.basics
import Natural.peano
import Natural.arithmetic

theorem bad (a b : Natural) (h : successor(a) = successor(b)) : a = b :=
  Natural.successor_injective(a, h)
