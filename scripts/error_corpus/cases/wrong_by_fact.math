module corpus.wrong_by_fact

import axioms
import Logic.basics
import Equality.basics
import Natural.basics
import Natural.peano
import Natural.arithmetic

theorem bad (h : 0 = 0) : successor(zero) = zero := {
  claim stepped : successor(zero) = zero by (0 = 0);
  stepped
}
