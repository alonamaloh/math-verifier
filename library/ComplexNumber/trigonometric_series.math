-- ComplexNumber/trigonometric_series.math
--
-- The real power series of cosine and sine, harvested from the complex
-- exponential. Since cos x = Re(exp(ix)) and Re commutes with the
-- (convergent) partial sums, cos x is the limit of the real series
-- whose k-th term is Re of the k-th term of exp(ix); likewise sin x
-- with Im. The k-th term, evaluated through the i-power 4-cycle:
--   Re(term_{2m})   = (1/(2m)!)·(−x²)^m,   Re(term_{2m+1}) = 0,
--   Im(term_{2m})   = 0,                   Im(term_{2m+1}) = (1/(2m+1)!)·x·(−x²)^m,
-- i.e. cos x = Σ (−1)^m x^{2m}/(2m)! and sin x = Σ (−1)^m x^{2m+1}/(2m+1)!
-- once the vanishing terms are dropped (done in the bracketing layer).

module ComplexNumber.trigonometric_series

import Logic.basics
import Logic.exists
import Logic.product
import Logic.quotient
import Logic.excluded_middle
import axioms
import Equality.basics
import Natural.basics
import Natural.peano
import Natural.order
import Natural.add_order
import Natural.arithmetic
import Natural.maximum
import Natural.compare
import Natural.subtraction
import Natural.factorial
import Integer.basics
import Integer.embedding
import Rational.basics
import Rational.embedding
import Rational.addition
import Rational.multiplication
import Rational.negation
import Rational.ring
import Rational.order
import Rational.order_arithmetic
import Rational.order_multiplication
import Rational.halve
import Rational.positive
import Rational.linearity
import Rational.minimum
import Rational.reciprocal
import Rational.archimedean
import Set.basics
import Algebra.ring
import Algebra.ring_bundle
import Algebra.ring_lemmas
import Real.interface
import Polynomial.basics
import Polynomial.addition
import Polynomial.negation
import Polynomial.multiplication
import Polynomial.monomial
import RingModulo.basics
import RingModulo.operations
import ComplexNumber.basics
import ComplexNumber.coordinates
import ComplexNumber.coordinate_homomorphism
import ComplexNumber.defining_polynomial_vanishes
import ComplexNumber.coordinate_map
import ComplexNumber.coordinate_multiplicative
import ComplexNumber.reconstruction
import ComplexNumber.of_coordinates
import ComplexNumber.coordinate_negate
import ComplexNumber.embedding
import ComplexNumber.modulus
import ComplexNumber.modulus_multiplicative
import ComplexNumber.completeness
import ComplexNumber.limits
import ComplexNumber.exponential
import ComplexNumber.conjugation
import ComplexNumber.trigonometry
import ComplexNumber.exponential_imaginary

-- ----------------------------------------------------------------------
-- A real scalar (embedded) pulls out of the coordinates of a product.

theorem ComplexNumber.realPart_from_real_multiply (r : Real) (w : ComplexNumber)
        : ComplexNumber.realPart(ComplexNumber.from_real(r) * w)
          = r * ComplexNumber.realPart(w) :=
  ComplexNumber.realPart(ComplexNumber.from_real(r) * w)
     = ComplexNumber.realPart(ComplexNumber.from_real(r)) * ComplexNumber.realPart(w)
       - ComplexNumber.imaginaryPart(ComplexNumber.from_real(r))
             * ComplexNumber.imaginaryPart(w)
     = r * ComplexNumber.realPart(w)
       - ComplexNumber.imaginaryPart(ComplexNumber.from_real(r))
             * ComplexNumber.imaginaryPart(w)
           by ComplexNumber.realPart_from_real
     = r * ComplexNumber.realPart(w) - Real.zero * ComplexNumber.imaginaryPart(w)
     = r * ComplexNumber.realPart(w)
           by ring

theorem ComplexNumber.imaginaryPart_from_real_multiply (r : Real) (w : ComplexNumber)
        : ComplexNumber.imaginaryPart(ComplexNumber.from_real(r) * w)
          = r * ComplexNumber.imaginaryPart(w) :=
  ComplexNumber.imaginaryPart(ComplexNumber.from_real(r) * w)
     = ComplexNumber.realPart(ComplexNumber.from_real(r)) * ComplexNumber.imaginaryPart(w)
       + ComplexNumber.imaginaryPart(ComplexNumber.from_real(r))
             * ComplexNumber.realPart(w)
     = r * ComplexNumber.imaginaryPart(w)
       + ComplexNumber.imaginaryPart(ComplexNumber.from_real(r))
             * ComplexNumber.realPart(w)
           by ComplexNumber.realPart_from_real
     = r * ComplexNumber.imaginaryPart(w) + Real.zero * ComplexNumber.realPart(w)
     = r * ComplexNumber.imaginaryPart(w)
           by ring

-- ----------------------------------------------------------------------
-- The k-th term of the cosine/sine series: Re/Im of the k-th term of
-- exp(ix), with the real coefficient 1/k! pulled out.

-- The coefficient that the term carries: 1/k!.
definition Real.exponentialCoefficient (k : Natural) : Real :=
  1 / (k! : Real)

theorem ComplexNumber.realPart_exponentialTerm_imaginaryAxis (x : Real) (k : Natural)
        : ComplexNumber.realPart(
              ComplexNumber.exponentialTerm(
                  ComplexNumber.i * ComplexNumber.from_real(x), k))
          = Real.exponentialCoefficient(k)
            * ComplexNumber.realPart(
                  (ComplexNumber.i * ComplexNumber.from_real(x)) ^ k) :=
  by ComplexNumber.realPart_from_real_multiply

theorem ComplexNumber.imaginaryPart_exponentialTerm_imaginaryAxis (x : Real) (k : Natural)
        : ComplexNumber.imaginaryPart(
              ComplexNumber.exponentialTerm(
                  ComplexNumber.i * ComplexNumber.from_real(x), k))
          = Real.exponentialCoefficient(k)
            * ComplexNumber.imaginaryPart(
                  (ComplexNumber.i * ComplexNumber.from_real(x)) ^ k) :=
  by ComplexNumber.imaginaryPart_from_real_multiply

-- ----------------------------------------------------------------------
-- The even/odd evaluations, from the i-power 4-cycle.

-- Re(term_{2m}) = (1/(2m)!)·(−x²)^m.
theorem Real.cosine_term_even (x : Real) (m : Natural)
        : ComplexNumber.realPart(
              ComplexNumber.exponentialTerm(
                  ComplexNumber.i * ComplexNumber.from_real(x), 2 * m))
          = Real.exponentialCoefficient(2 * m) * (-(x * x)) ^ m := {
  ComplexNumber.realPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ (2 * m))
      = (-(x * x)) ^ m
    ∧ ComplexNumber.imaginaryPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ (2 * m))
      = Real.zero
    ∧ ComplexNumber.realPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ successor(2 * m))
      = Real.zero
    ∧ ComplexNumber.imaginaryPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ successor(2 * m))
      = x * (-(x * x)) ^ m
      by ComplexNumber.imaginaryAxis_power_coordinates;
  ComplexNumber.realPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ (2 * m))
      = (-(x * x)) ^ m as evenReal;
  ComplexNumber.realPart(
           ComplexNumber.exponentialTerm(
               ComplexNumber.i * ComplexNumber.from_real(x), 2 * m))
     = Real.exponentialCoefficient(2 * m)
       * ComplexNumber.realPart(
             (ComplexNumber.i * ComplexNumber.from_real(x)) ^ (2 * m))
     = Real.exponentialCoefficient(2 * m) * (-(x * x)) ^ m
           by evenReal
}

-- Re(term_{2m+1}) = 0.
theorem Real.cosine_term_odd (x : Real) (m : Natural)
        : ComplexNumber.realPart(
              ComplexNumber.exponentialTerm(
                  ComplexNumber.i * ComplexNumber.from_real(x), (2 * m + 1)))
          = Real.zero := {
  ComplexNumber.realPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ (2 * m))
      = (-(x * x)) ^ m
    ∧ ComplexNumber.imaginaryPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ (2 * m))
      = Real.zero
    ∧ ComplexNumber.realPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ successor(2 * m))
      = Real.zero
    ∧ ComplexNumber.imaginaryPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ successor(2 * m))
      = x * (-(x * x)) ^ m
      by ComplexNumber.imaginaryAxis_power_coordinates;
  ComplexNumber.realPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ successor(2 * m))
      = Real.zero as oddReal;
  ComplexNumber.realPart(
           ComplexNumber.exponentialTerm(
               ComplexNumber.i * ComplexNumber.from_real(x), (2 * m + 1)))
     = Real.exponentialCoefficient((2 * m + 1))
       * ComplexNumber.realPart(
             (ComplexNumber.i * ComplexNumber.from_real(x)) ^ (2 * m + 1))
     = Real.exponentialCoefficient((2 * m + 1)) * Real.zero
           by oddReal
     = Real.zero
           by ring
}

-- Im(term_{2m}) = 0.
theorem Real.sine_term_even (x : Real) (m : Natural)
        : ComplexNumber.imaginaryPart(
              ComplexNumber.exponentialTerm(
                  ComplexNumber.i * ComplexNumber.from_real(x), 2 * m))
          = Real.zero := {
  ComplexNumber.realPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ (2 * m))
      = (-(x * x)) ^ m
    ∧ ComplexNumber.imaginaryPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ (2 * m))
      = Real.zero
    ∧ ComplexNumber.realPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ successor(2 * m))
      = Real.zero
    ∧ ComplexNumber.imaginaryPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ successor(2 * m))
      = x * (-(x * x)) ^ m
      by ComplexNumber.imaginaryAxis_power_coordinates;
  ComplexNumber.imaginaryPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ (2 * m))
      = Real.zero as evenImaginary;
  ComplexNumber.imaginaryPart(
           ComplexNumber.exponentialTerm(
               ComplexNumber.i * ComplexNumber.from_real(x), 2 * m))
     = Real.exponentialCoefficient(2 * m)
       * ComplexNumber.imaginaryPart(
             (ComplexNumber.i * ComplexNumber.from_real(x)) ^ (2 * m))
     = Real.exponentialCoefficient(2 * m) * Real.zero
           by evenImaginary
     = Real.zero
           by ring
}

-- Im(term_{2m+1}) = (1/(2m+1)!)·x·(−x²)^m.
theorem Real.sine_term_odd (x : Real) (m : Natural)
        : ComplexNumber.imaginaryPart(
              ComplexNumber.exponentialTerm(
                  ComplexNumber.i * ComplexNumber.from_real(x), (2 * m + 1)))
          = Real.exponentialCoefficient((2 * m + 1)) * (x * (-(x * x)) ^ m) := {
  ComplexNumber.realPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ (2 * m))
      = (-(x * x)) ^ m
    ∧ ComplexNumber.imaginaryPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ (2 * m))
      = Real.zero
    ∧ ComplexNumber.realPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ successor(2 * m))
      = Real.zero
    ∧ ComplexNumber.imaginaryPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ successor(2 * m))
      = x * (-(x * x)) ^ m
      by ComplexNumber.imaginaryAxis_power_coordinates;
  ComplexNumber.imaginaryPart(
          (ComplexNumber.i * ComplexNumber.from_real(x)) ^ successor(2 * m))
      = x * (-(x * x)) ^ m as oddImaginary;
  ComplexNumber.imaginaryPart(
           ComplexNumber.exponentialTerm(
               ComplexNumber.i * ComplexNumber.from_real(x), (2 * m + 1)))
     = Real.exponentialCoefficient((2 * m + 1))
       * ComplexNumber.imaginaryPart(
             (ComplexNumber.i * ComplexNumber.from_real(x)) ^ (2 * m + 1))
     = Real.exponentialCoefficient((2 * m + 1)) * (x * (-(x * x)) ^ m)
           by oddImaginary
}

-- ----------------------------------------------------------------------
-- cos and sin as the limits of their real series. Re/Im commute with
-- the convergent partial sums of exp(ix): the real partial sums of
-- Re(term) converge to Re(exp(ix)) = cos x.

theorem Real.cosine_series_converges (x : Real)
        : Real.SequenceConverges(
              Real.partialSum(
                  (k : Natural) ↦
                      ComplexNumber.realPart(
                          ComplexNumber.exponentialTerm(
                              ComplexNumber.i * ComplexNumber.from_real(x), k))),
              cos(x)) := {
  ComplexNumber.SequenceConverges(
      ComplexNumber.partialSum(
          ComplexNumber.exponentialTerm(
              ComplexNumber.i * ComplexNumber.from_real(x))),
      ComplexNumber.exponential(ComplexNumber.i * ComplexNumber.from_real(x)))
      by ComplexNumber.exponential_converges;
  Real.SequenceConverges(
      (n : Natural) ↦
          ComplexNumber.realPart(
              ComplexNumber.partialSum(
                  ComplexNumber.exponentialTerm(
                      ComplexNumber.i * ComplexNumber.from_real(x)),
                  n)),
      ComplexNumber.realPart(
          ComplexNumber.exponential(
              ComplexNumber.i * ComplexNumber.from_real(x))))
      by ComplexNumber.SequenceConverges.realPart(
             s := ComplexNumber.partialSum(
                      ComplexNumber.exponentialTerm(
                          ComplexNumber.i * ComplexNumber.from_real(x))));
  ∀ (n : Natural).
      ComplexNumber.realPart(
            ComplexNumber.partialSum(
                ComplexNumber.exponentialTerm(
                    ComplexNumber.i * ComplexNumber.from_real(x)),
                n))
        = Real.partialSum(
              (k : Natural) ↦
                  ComplexNumber.realPart(
                      ComplexNumber.exponentialTerm(
                          ComplexNumber.i * ComplexNumber.from_real(x), k)),
              n)
      by ComplexNumber.realPart_partialSum;
  -- cos x is exactly Re(exp(ix)) by definition.
  Real.SequenceConverges(
      Real.partialSum(
          (k : Natural) ↦
              ComplexNumber.realPart(
                  ComplexNumber.exponentialTerm(
                      ComplexNumber.i * ComplexNumber.from_real(x), k))),
      ComplexNumber.realPart(
          ComplexNumber.exponential(
              ComplexNumber.i * ComplexNumber.from_real(x))))
      by Real.SequenceConverges.of_pointwise(
             s := (n : Natural) ↦
                      ComplexNumber.realPart(
                          ComplexNumber.partialSum(
                              ComplexNumber.exponentialTerm(
                                  ComplexNumber.i * ComplexNumber.from_real(x)),
                              n)),
             t := Real.partialSum(
                      (k : Natural) ↦
                          ComplexNumber.realPart(
                              ComplexNumber.exponentialTerm(
                                  ComplexNumber.i * ComplexNumber.from_real(x), k))));
  done
}

theorem Real.sine_series_converges (x : Real)
        : Real.SequenceConverges(
              Real.partialSum(
                  (k : Natural) ↦
                      ComplexNumber.imaginaryPart(
                          ComplexNumber.exponentialTerm(
                              ComplexNumber.i * ComplexNumber.from_real(x), k))),
              sin(x)) := {
  ComplexNumber.SequenceConverges(
      ComplexNumber.partialSum(
          ComplexNumber.exponentialTerm(
              ComplexNumber.i * ComplexNumber.from_real(x))),
      ComplexNumber.exponential(ComplexNumber.i * ComplexNumber.from_real(x)))
      by ComplexNumber.exponential_converges;
  Real.SequenceConverges(
      (n : Natural) ↦
          ComplexNumber.imaginaryPart(
              ComplexNumber.partialSum(
                  ComplexNumber.exponentialTerm(
                      ComplexNumber.i * ComplexNumber.from_real(x)),
                  n)),
      ComplexNumber.imaginaryPart(
          ComplexNumber.exponential(
              ComplexNumber.i * ComplexNumber.from_real(x))))
      by ComplexNumber.SequenceConverges.imaginaryPart(
             s := ComplexNumber.partialSum(
                      ComplexNumber.exponentialTerm(
                          ComplexNumber.i * ComplexNumber.from_real(x))));
  ∀ (n : Natural).
      ComplexNumber.imaginaryPart(
            ComplexNumber.partialSum(
                ComplexNumber.exponentialTerm(
                    ComplexNumber.i * ComplexNumber.from_real(x)),
                n))
        = Real.partialSum(
              (k : Natural) ↦
                  ComplexNumber.imaginaryPart(
                      ComplexNumber.exponentialTerm(
                          ComplexNumber.i * ComplexNumber.from_real(x), k)),
              n)
      by ComplexNumber.imaginaryPart_partialSum;
  Real.SequenceConverges(
      Real.partialSum(
          (k : Natural) ↦
              ComplexNumber.imaginaryPart(
                  ComplexNumber.exponentialTerm(
                      ComplexNumber.i * ComplexNumber.from_real(x), k))),
      ComplexNumber.imaginaryPart(
          ComplexNumber.exponential(
              ComplexNumber.i * ComplexNumber.from_real(x))))
      by Real.SequenceConverges.of_pointwise(
             s := (n : Natural) ↦
                      ComplexNumber.imaginaryPart(
                          ComplexNumber.partialSum(
                              ComplexNumber.exponentialTerm(
                                  ComplexNumber.i * ComplexNumber.from_real(x)),
                              n)),
             t := Real.partialSum(
                      (k : Natural) ↦
                          ComplexNumber.imaginaryPart(
                              ComplexNumber.exponentialTerm(
                                  ComplexNumber.i * ComplexNumber.from_real(x), k))));
  done
}
