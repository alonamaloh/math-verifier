-- Regression: `obtain`/`cases` on a scrutinee whose TYPE mentions a local
-- `let`-bound alias now ζ-unfolds the let and finds the inductive head, so the
-- obtain just works (it used to error "type's head is not an inductive
-- constant after normalisation").
module Test.obtain_through_let

import Logic.basics
import Natural.basics

theorem Test.obtain_let (P : Proposition) (hp : P) : P := {
  let Q : Proposition := P ∧ P;
  claim hq : Q by And.introduction(hp, hp);
  obtain ⟨a, b⟩ from hq;
  a
}
