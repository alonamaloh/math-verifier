-- D Phase-1 stage 1: an implementation module parses and builds as an
-- ordinary module; the `implements` relation is recorded (consumed by
-- the interface side in stage 2).
implementation module Test.implementation_module_test implements Test.counter_interface

import Logic.basics
import Equality.basics
import Natural.basics
import Natural.arithmetic

definition Test.counter : Natural := 0

theorem Test.counter_is_zero : Test.counter = 0 :=
  done
