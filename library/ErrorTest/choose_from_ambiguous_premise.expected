# An inferred-citation destructure (choose-from-lemma / cases-by) refuses to
# guess among hypotheses that pin different lemma arguments, listing the
# candidates (requireUnambiguousDischarge, inferCallWithHoles step 5c).
ambiguous `by Natural.subtraction_witness` citation: the premise `? ≤ ?` is matched by several hypotheses that pin different arguments
iBelow : successor @i ≤ @p
jLessOrEqual : @j ≤ @i
pass the lemma's arguments explicitly
