-- The size guard fires before any per-case proof search.
module ErrorTest.finite_check_over_cap

import Logic.basics
import Natural.finite_range

theorem ErrorTest.tooManyCases (P : ℕ → Proposition)
        : ∀ (n : ℕ). 0 ≤ n → n < 257 → P(n) :=
  by finite_check n from 0 until 257

