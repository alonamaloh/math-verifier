-- PENDING (PLAN_ELABORATOR_ERGONOMICS.md step E1). An integer matrix entry
-- presents as `CommutativeRing.carrier(Integer.commutative_ring_bundle)` — a
-- projection over a CLOSED bundle, which must reduce to Integer for dispatch.
-- It does not, so the bare numeral wins the race and selects Natural.LessThan.
--
-- carrierProjectionField (desugar_eliminators.cpp:473) already computes the
-- right answer; it is merely gated behind primary dispatch succeeding.
--
-- When E1 lands, DELETE this file and move the probe into
-- library/Test/expected_carrier_propagation_test.math.
module ErrorTest.carrier_pending_bundled_relation

import Algebra.escalation

definition ErrorTest.pending {n : ℕ}
        (M : Matrix(Integer.commutative_ring_bundle, n, n))
        (i : NaturalsBelow(n)) : Proposition :=
  M(i, i) < 3
