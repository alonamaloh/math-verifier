# A comma after the `substituting` equation is diagnosed at the source with
# a clear message that names the single-equation rule and points to the
# one-substituting-per-step idiom — not a downstream "expected expression".
`substituting` takes a single equation
one `substituting` per step
