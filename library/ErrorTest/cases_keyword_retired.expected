# The retired `cases <scrutinee>` pattern-match fails at parse time, teaching
# every replacement: by cases / by induction (proofs), let ⟨…⟩ (values),
# by_representatives (quotient representatives).
the `cases <scrutinee>` pattern-match was retired
by cases
by induction on x
let ⟨…⟩ := x
by_representatives x as
