# A construction-internal name (a sealing-note skip) is ABSENT from the
# sealed cache, so a consumer citing it fails at name lookup — no
# advisory lint needed.
unknown lemma `Real.bisectionLeft_below_witness`
