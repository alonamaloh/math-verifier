-- Using a reserved word (`witness`) as a binder name. The parser
-- rejects it with a generic "expected at least one name in binder",
-- never saying that `witness` is reserved — a recurring papercut.
module ErrorTest.reserved_word_binder

import Natural.basics
import Natural.peano

theorem ErrorTest.uses_reserved (witness : Natural) : Natural :=
  witness
