-- The bounded concrete evaluator's initial cap is five.

module ErrorTest.matrix_compute_above_bound

import Algebra.matrix_ring

theorem ErrorTest.matrix_compute_declines_dimension_six
        {r : CommutativeRing}
        (A : Matrix(r, 6, 6))
        : A = A := by matrix_compute
