# FIXED (fix #3): a `divides` goal that reached the error δ-unfolded is
# re-folded for display (errors.cpp refoldForDisplay) and rendered in the
# user's `∣` notation (printer.cpp), instead of its raw `Exists` body.
the `Natural.multiply_commutative` citation does not prove this goal
goal:        d ∣ n
!Exists.{0}
