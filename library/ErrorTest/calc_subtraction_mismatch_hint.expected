# FIXED: a calc-step relation mismatch involving subtraction now hints that
# `a - b` and `a + -b` are structurally distinct to the matcher (try `ring`),
# instead of dumping two relations that print alike with no explanation.
this step's justification proves a different relation
this step involves subtraction
close the step with `ring`
