-- PENDING (PLAN_ELABORATOR_ERGONOMICS.md step E2). `b = -m` for `b : ℤ`,
-- `m : ℕ` must elaborate without `-(m : ℤ)`. Same node as
-- carrier_pending_unary_numeral, but a VARIABLE rather than a literal — the
-- fix must not be numeral-specific, so both are pinned.
--
-- When E2 lands, DELETE this file and move the probe into
-- library/Test/expected_carrier_propagation_test.math.
module ErrorTest.carrier_pending_unary_natural

import Integer.basics
import Integer.negation
import Integer.ring
import Integer.embedding

definition ErrorTest.pending (b : ℤ) (m : ℕ) : Proposition := b = -m
