# FIXED by requireUnambiguousDischarge (inferCallWithHoles step 5c): the
# cases-by citation reports the ambiguous premise with both candidates at
# the citation site, instead of silently picking one and failing far away.
ambiguous `by Integer.multiply_eq_zero_implies` citation: the premise `? * ? = Integer.zero` is matched by several hypotheses that pin different arguments
bbZero : @b * @b = Integer.zero
aaZero : @a * @a = Integer.zero
pass the lemma's arguments explicitly
