-- §9 error 6: a series in term position (here an operand of +).
module ErrorTest.series_term_position

import Logic.basics
import Equality.basics
import Natural.basics
import Natural.arithmetic
import Algebra.monoid
import Algebra.aggregation
import Rational.basics
import Real.basics
import Real.embedding
import Real.addition
import Real.multiplication
import Real.power

theorem ErrorTest.termPosition (S : Real)
        : (Real.power(2, 0) + Real.power(2, 1) + ... + Real.power(2, k) + ...) + 1 = S :=
  done
