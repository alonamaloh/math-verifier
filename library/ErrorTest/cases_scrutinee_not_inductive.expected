# FIXED: a cases/obtain scrutinee whose type doesn't reduce to an inductive
# (after ζ-unfolding local lets) now names the offending normalised type and
# explains it must be inductive — instead of the bare "head is not an inductive
# constant after normalisation".
its type does not reduce to an inductive
need an inductive type
did not unfold
