# FIXED: `decide` desugars to `Logic.classical_decidable`, which moved out of
# the universally-imported axioms.math into Natural.classical_decidable. A
# `decide` without that import now names the missing import instead of a bare
# "unknown identifier".
unknown identifier 'Logic.classical_decidable'
add `import Natural.classical_decidable`
