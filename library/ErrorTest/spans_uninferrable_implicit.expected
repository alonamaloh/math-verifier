# A `Spans(fam)` declared type whose leading implicits cannot be inferred
# fails LOUDLY at the type — naming the un-inferred leading arguments and
# telling the user to supply them — instead of silently building a
# malformed application that later yields a misleading "conclusion is
# about Or" citation error.
could not infer all leading arguments of 'VectorSpace.Spans'
is unassigned
Provide the missing argument(s) explicitly
