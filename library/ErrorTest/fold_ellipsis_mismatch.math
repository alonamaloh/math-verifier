-- §9 error 3: the general term does not generate the written prefix —
-- the message shows the candidate term function and the generated vs
-- written terms.
module ErrorTest.fold_ellipsis_mismatch

import Logic.basics
import Equality.basics
import Natural.basics
import Natural.arithmetic
import Algebra.monoid
import Algebra.aggregation

theorem ErrorTest.mismatch (n : Natural) : (2 + 4 + ... + 3 * n) = 0 :=
  done
