# A pattern-match theorem proof fails at parse time, pointing at the
# `by induction on <proof> { case … }` replacement and noting that
# `definition`s keep pattern-match clauses.
no longer accepted for a `theorem`
by induction on <proof>
remain available for `definition`s
