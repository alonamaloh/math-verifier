# The --goal-at report must survive the downstream elaboration failure,
# land on the statement enclosing the queried continuation line, and show
# the hypotheses (including the first claim's named result) and the goal.
goal at line 16:
h : a = b
stepOne : a + b = b + a
⊢ a + b = b + b
# The downstream error itself must still be reported.
unknown lemma `ErrorTest.no_such_lemma`
