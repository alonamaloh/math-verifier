# A `cases` on a computed expression (a function application) is rejected — the
# scrutinee must be a variable. The message must say so and steer to the
# condition forms (`if` / `by cases`) or an argument-matching helper.
pattern-match `cases` on a computed expression is not supported
scrutinee must be a variable
