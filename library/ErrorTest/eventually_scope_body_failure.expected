# The failing claim inside the scope must be named ...
claim `Real.absolute_value ((s m) - limit) <
# ... reached through the scope's frame, which quotes the phrase written ...
for m sufficiently large: at line
# ... and NOT masked by the stated-proposition fallback.
!needs an expected type from context
