# FIXED: an over-long obtain/⟨…⟩ pattern (a trailing component right-nesting onto
# a proof) now names the type being wrongly destructured and points at the
# component-count cause, instead of leaking "index N must be a local variable".
cannot destructure a value of type 'Equality'
pattern has MORE components than the source provides
drop the extra component(s)
