# The fingerprint mismatch is real, but an unreduced definition application
# sat among the atoms — the verdict must be softened, not a flat FALSE.
the two sides do not agree mod 2^61 - 1
APPLICATION was treated as an opaque atom
!the identity is FALSE
