-- The bounded concrete evaluator must decline symbolic dimensions.

module ErrorTest.matrix_compute_symbolic_dimension

import Algebra.matrix_ring

theorem ErrorTest.matrix_compute_declines_symbolic_dimension
        {r : CommutativeRing} {n : ℕ}
        (A : Matrix(r, n, n))
        : A = A := by matrix_compute
