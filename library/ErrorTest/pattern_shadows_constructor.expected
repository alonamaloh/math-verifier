# FIXED: a pattern binder shadowing a constructor of the matched type in a
# non-first slot is reported at the pattern (naming the constructor, the type,
# and the position), with the fix — rename, or move the constructor to the
# scrutinee position. Was: a distant byte-identical type mismatch.
pattern variable `zero` at position 2 shadows constructor `zero` of `Natural`
does not match the constructor
Rename the binder, or move the constructor pattern to the first
