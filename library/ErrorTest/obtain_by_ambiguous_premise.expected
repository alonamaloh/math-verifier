# FIXED by requireUnambiguousDischarge (inferCallWithHoles step 5c): an
# obtain-by/cases-by citation refuses to guess among hypotheses that pin
# different lemma arguments, listing the candidates.
ambiguous `by Natural.subtraction_witness` citation: the premise `? ≤ ?` is matched by several hypotheses that pin different arguments
iBelow : successor @i ≤ @p
jLessOrEqual : @j ≤ @i
pass the lemma's arguments explicitly
