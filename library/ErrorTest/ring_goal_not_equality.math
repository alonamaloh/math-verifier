-- `ring` on a non-equality goal must name the goal's relation readably
-- and point at the calc-chain idiom.

module ErrorTest.ring_goal_not_equality

import Logic.basics
import Equality.basics
import Natural.basics
import Natural.peano
import Natural.order

theorem ErrorTest.not_equality (a b : Natural) : a + b ≤ b + a := ring
