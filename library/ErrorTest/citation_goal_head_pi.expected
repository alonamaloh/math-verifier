# The goal is a `∀`/`→` statement; the head-mismatch line must describe
# that shape, not leak the `<unknown>` placeholder.
the `h` citation does not prove this goal
its conclusion is about `Equality` but the goal is about a `∀`/`→` statement
# The raw placeholder must NOT appear.
!about `<unknown>`
